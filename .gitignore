.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn

# Keypair — LỘ LÀ MẤT QUYỀN KIỂM SOÁT PROGRAM.
# Ai có file này thì upgrade được contract của bạn, tức là đổi được code đang
# giữ tiền thật. Không bao giờ commit.
*.json
!package.json
!tsconfig.json
!Anchor.toml
keypairs/
*-keypair.json

# Skill cài từ npx skills — không phải code của dự án
.agents/
.claude/
skills-lock.json

.env
.env.*

# Surfpool local state cache — mainnet fork data, not source
.surfpool/

# Internal design docs — working notes, not published
ARCHITECTURE.md
SPEC.md
DESIGN-*.md
